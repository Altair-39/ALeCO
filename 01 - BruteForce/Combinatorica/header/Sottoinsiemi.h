#ifndef SOTTOINSIEMI_H
#define SOTTOINSIEMI_H

#include <stdbool.h>
#include <stddef.h>

void risposta(int *a, bool *r, int j, int length);
void sottoinsiemi(int *a, int length);
void toString(int *a, bool *r, int length);
void risposte(int *a, int *j, int k, int length);

#endif // SOTTOINSIEMI_H
