#ifndef DISPOSIZIONIRPT_H
#define DISPOSIZIONIRPT_H

#include <stddef.h>

extern size_t count;

void risposte(size_t *a, size_t *soluzioni, size_t j, size_t k, size_t length);

#endif // DISPOSIZIONIRPT_H
